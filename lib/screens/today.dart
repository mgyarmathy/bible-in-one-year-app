import 'package:flutter/material.dart';

class TodayScreen extends StatelessWidget {
  const TodayScreen() : super();

  @override
  Widget build(BuildContext context) {
    return Text("today");
  }
}
